"use client";

import { useParams, useRouter } from "next/navigation";
import { useEffect, useMemo, useState } from "react";
import { authedFetch } from "@/lib/api/authedFetch";
type R=Record<string,any>;const t=(v:any,f="-")=>String(v??"").trim()||f;const n=(v:any)=>String(v??"").trim().toLowerCase();const okReview=(v:any)=>["approved","accepted","goedgekeurd","akkoord","resolved","afgehandeld","closed"].includes(n(v));const date=(v:any)=>{if(!v)return"-";const d=new Date(String(v).length===10?`${v}T12:00:00`:v);return Number.isNaN(d.getTime())?t(v):d.toLocaleDateString("nl-NL")};function sideData(row:R,ctx:R[]){const pn=Number(row?.partij_nr);const c=ctx.find(x=>Number(x?.partij_nr)===pn)||{};const s=row?.hoek==="blauw"?"blauw":"rood";return{partij:pn>0?`P${pn}`:t(row?.toernooi_code,"T"),naam:t(c?.[`${s}_naam_fp`]??c?.[`${s}_naam_mm`]??row?.fighter_name,"Onbekend"),gym:t(c?.[`${s}_gym_mm`]??c?.[`${s}_gym_fp`]??c?.[`${s}_sportschool`],"-"),reden:t(row?.boodschap??row?.rule,"-")}}
export default function MatchmakerEindrapportPage(){const{matchmakingId}=useParams<{matchmakingId:string}>();const router=useRouter();const[data,setData]=useState<any>();const[error,setError]=useState("");useEffect(()=>{(async()=>{try{const r=await authedFetch(`/api/rapport/matchmaker-eindrapport?matchmaking_id=${encodeURIComponent(matchmakingId)}`,{cache:"no-store"});const j=await r.json();if(!r.ok||!j?.ok)throw new Error(j?.error||"Eindrapport laden mislukt");setData(j)}catch(e:any){setError(e?.message||String(e))}})()},[matchmakingId]);const s=useMemo(()=>{if(!data)return null;const ctx:R[]=data.bout_context||[],res:R[]=data.resultaten||[];const open=res.filter(x=>!okReview(x.review_status)&&!["ok","info"].includes(n(x.resultaat)));const cat=(q:(x:R)=>boolean)=>open.filter(q).map(x=>sideData(x,ctx));const keurRows=open.filter(x=>{const c=String(x.rule_code||"").toUpperCase();return c.includes("KEURMERK")||c.includes("SPORTSCHOOL_NIET_GEVONDEN")});const keur=[...keurRows.reduce((m:Map<string,any>,x:R)=>{const pn=Number(x.partij_nr),c=ctx.find(y=>Number(y.partij_nr)===pn)||{},side=x.hoek==="blauw"?"blauw":"rood",gym=t(c?.[`${side}_gym_mm`]??c?.[`${side}_gym_fp`]??c?.[`${side}_sportschool`],"-"),matched=t(c?.[`gematchte_sportschool_${side}`],"-"),key=n(gym),cur=m.get(key)||{gym,matched,partijen:new Set<string>()};cur.partijen.add(pn>0?`P${pn}`:t(x.toernooi_code,"T"));if(cur.matched==="-"&&matched!=="-")cur.matched=matched;m.set(key,cur);return m},new Map<string,any>()).values()].map((x:any)=>({...x,partijen:[...x.partijen].join(", ")}));const unique=(list:any[])=>[...new Map(list.map(x=>[`${x.partij}|${x.naam}|${x.reden}`,x])).values()];return{ctx,lic:cat(x=>String(x.rule_code||"").toUpperCase().includes("LICENT")),nova:cat(x=>{const c=String(x.rule_code||"").toUpperCase();return c.includes("FIGHTPASPOORT")||c.includes("GEEN_FIGHTPASSPORT_INFO")}),start:cat(x=>String(x.rule_code||"").toUpperCase().includes("STARTVERBOD")),keur,verb:unique(cat(x=>n(x.resultaat)==="verbod"&&!String(x.rule_code||"").toUpperCase().includes("STARTVERBOD"))),disp:unique(cat(x=>n(x.resultaat)==="dispensatie"||String(x.rule_code||"").toUpperCase().includes("DISPENSATIE"))),akkoord:open.length===0}},[data]);if(error)return <main className="p-5 text-red-300">{error}</main>;if(!data||!s)return <main className="p-5 text-zinc-300">Eindrapport laden...</main>;const Box=({title,list}:{title:string,list:any[]})=><div className="overflow-hidden rounded border border-zinc-700 bg-[#151518]"><div className="flex justify-between bg-zinc-700 px-2 py-1 text-[11px] font-black uppercase"><span>{title}</span><span>{list.length}</span></div>{list.map((x:any,i:number)=><div key={i} className="grid grid-cols-[42px_1fr] gap-2 border-t border-zinc-800 px-2 py-1 text-[11px]"><b className="text-[#ff6a2a]">{x.partij}</b><span><b>{x.naam}</b>{x.gym!=="-"?` · ${x.gym}`:""}</span></div>)}</div>;return <main className="min-h-screen bg-[#101012] p-3 text-white"><section className="mx-auto max-w-[1200px] overflow-hidden rounded-lg border border-zinc-700"><header className="flex items-center gap-3 bg-[#1b1b1f] px-4 py-2"><div><div className="text-[10px] font-black uppercase tracking-widest text-[#ff4d00]">Matchmaker eindrapport</div><h1 className="text-lg font-black">{t(data.event?.naam)}</h1><div className="text-[11px] text-zinc-400">{date(data.event?.datum)} · {t(data.event?.locatie)} · {s.ctx.length} partijen</div></div><div className={`ml-auto rounded px-4 py-2 text-sm font-black ${s.akkoord?"bg-zinc-200 text-zinc-950":"bg-[#ff4d00] text-white"}`}>{s.akkoord?"EVENT AKKOORD":"EVENT GEBLOKKEERD"}</div><button onClick={()=>router.push(`/dashboard/matchmaker/matchmaking/${matchmakingId}`)} className="rounded border border-zinc-600 px-3 py-1 text-xs font-bold">Terug</button></header><div className="space-y-2 p-3"><section className="grid gap-2 md:grid-cols-2"><Box title="Licentie" list={s.lic}/><Box title="Geen VA / FightPassport" list={s.nova}/></section><section className="grid gap-2 md:grid-cols-2"><div className="overflow-hidden rounded border border-zinc-700 bg-[#151518]"><div className="flex justify-between bg-zinc-700 px-2 py-1 text-[11px] font-black uppercase"><span>Geen geldig keurmerk</span><span>{s.keur.length}</span></div>{s.keur.map((x:any,i:number)=><div key={i} className="grid grid-cols-[90px_1fr] gap-2 border-t border-zinc-800 px-2 py-1 text-[11px]"><b className="text-[#ff6a2a]">{x.partijen}</b><span><b>{x.gym}</b>{x.matched!=="-"&&n(x.matched)!==n(x.gym)?<span className="text-zinc-400"> · DB: {x.matched}</span>:null}</span></div>)}</div><Box title="Startverbod" list={s.start}/></section>{s.verb.length>0&&<section className="overflow-hidden rounded border border-[#ff4d00]"><div className="bg-[#ff4d00] px-2 py-1 text-xs font-black uppercase">Verboden partijen · {s.verb.length}</div>{s.verb.map((x:any,i:number)=><div key={i} className="grid grid-cols-[50px_1fr] gap-2 bg-zinc-100 px-2 py-1 text-[11px] text-zinc-950"><b>{x.partij}</b><span><b>{x.naam}</b>{x.gym!=="-"?` · ${x.gym}`:""}{x.reden!=="-"?<span className="ml-2 font-semibold text-[#b83a12]">— {x.reden.length>120?`${x.reden.slice(0,117)}...`:x.reden}</span>:null}</span></div>)}</section>}{s.disp.length>0&&<section className="overflow-hidden rounded border border-[#ff4d00]"><div className="bg-[#ff4d00] px-2 py-1 text-xs font-black uppercase">Blokkerende dispensaties · {s.disp.length}</div>{s.disp.map((x:any,i:number)=><div key={i} className="grid grid-cols-[50px_1fr] gap-2 bg-zinc-100 px-2 py-1 text-[11px] text-zinc-950"><b>{x.partij}</b><span><b>{x.naam}</b>{x.gym!=="-"?` · ${x.gym}`:""}{x.reden!=="-"?<span className="ml-2 font-semibold text-[#b83a12]">— {x.reden.length>120?`${x.reden.slice(0,117)}...`:x.reden}</span>:null}</span></div>)}</section>}</div></section></main>}
